# ram is volatile memory(data bhag jayega off krne p)
# hard disk is non-volatile memory
"""
'r' open file for reading(default mode)
'w' open file for writing
'x' create file if not exists/ if exists then operation failed
'a' add more content in a file to the end
't' open any file in text mode(default mode)
'b' binary mode
'+' read and write both(to update a file)

"""